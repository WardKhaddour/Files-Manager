const authRouter = require('./authRouter');
const passwordRouter = require('./passwordRouter');
const userRouter = require('./userRouter');

module.exports = { authRouter, passwordRouter, userRouter };
